from django.apps import AppConfig


class CentralAsiaConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.central_asia'
    verbose_name = 'Central Asia (parse)'
